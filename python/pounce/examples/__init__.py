"""Auditable application examples built on POUNCE's public interfaces.

The modules in this package are examples rather than general-purpose domain
libraries.  Import an example explicitly so optional dependencies are only
loaded when that example is used.
"""
